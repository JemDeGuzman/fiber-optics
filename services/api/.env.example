# services/api/.env.example
# Copy to services/api/.env and fill real values (do NOT commit secrets).

# Database (Postgres recommended in team/dev)
# Example: postgresql://user:password@localhost:5432/fiber_optics_dev
DATABASE_URL="postgresql://dev:dev@db:5432/rnbackend?schema=public"

# JWT for signing tokens (dev secret - rotate for prod)
JWT_SECRET="hinginmokayjemboi"

# Server port
API_PORT=4000

